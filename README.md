jproto is a dynamic packet/message decoding library.
In the context of this application, a protocol describes the format of a data.
jproto can be used to define file structure as well as packet structure.